# JobTrail — תקציר להעתקה לצ'אט חדש

העתיקי את כל מה שמתחת לקו לתוך ההודעה הראשונה בשיחה חדשה.

---

אני עובדת על פרויקט בשם **JobTrail**. הנה מה שצריך לדעת עליו:

## מה זה

אתר שעוקב אחרי עמודי הקריירה של חברות היי-טק בישראל, אוסף מהם משרות
אוטומטית, ומאפשר לחפש ולסנן אותן ולעקוב אחרי הגשות. אני סטודנטית שנה ג'
להנדסת תוכנה, וזה גם פרויקט אישי וגם פורטפוליו.

**חי בכתובת:** https://jobtrail-0xhs.onrender.com
**קוד:** https://github.com/liraztubul/job-search

## הטכנולוגיה

Node.js, **תלות ריצה אחת בלבד: `libsql`** (תואם better-sqlite3, סינכרוני).
בלי Express, בלי React — שרת HTTP על `node:http`, ולקוח HTML/CSS/JS רגיל.
האימות כולו `node:crypto` (scrypt לסיסמאות, HMAC לעוגיות סשן).

**זה עיקרון מרכזי בפרויקט, לא במקרה.** אל תציע לי להוסיף ספריות.

## ארכיטקטורה

```
web  ->  services  ->  data  ->  domain
```

- `server/domain/` — חוקים טהורים, לא מייבא כלום מהפרויקט
- `server/data/` — **כל ה-SQL, ורק שם**
- `server/services/` — חוקים עסקיים
- `server/web/` — HTTP בלבד
- `server/adapters/` — אדפטר אחד לכל **פלטפורמת** קריירה (לא לכל חברה),
  נרשמים אוטומטית. 15 אדפטרים, 37 חברות. הוספת חברה שהפלטפורמה שלה כבר
  נתמכת לא דורשת שורת קוד.

`client/` לעולם לא עושה `require`, `server/` לעולם לא מייבא מ-`client/`.
הם נפגשים ב-JSON API בלבד.

## איפה זה רץ

- **Render** (שכבה חינמית) מגישה את האתר, נרדמת אחרי 15 דקות
- **Turso** (libSQL מנוהל, אירלנד) מחזיקה את הנתונים — ל-Render אין דיסק קבוע
- **GitHub Actions** מריצה סריקה כל 3 שעות ישירות ל-Turso

## שלוש מלכודות שכבר עלו לי ביוקר

הטסטים רצים מול קובץ SQLite מקומי, והמסד בפרודקשן מרוחק. **שלושה דברים
עובדים מקומית ונשברים מרחוק, בלי שגיאה:**

1. **`db.transaction()` זורק** — BEGIN ו-COMMIT הן בקשות HTTP נפרדות. משתמשים
   ב-`UPDATE ... WHERE <עדיין לא נתפס>` ומסדרים את הפעולות כך שקריסה באמצע
   נכשלת לטובת הבטיחות.
2. **פרמטרים בעלי שם (`@name`) לא נקשרים בשקט** — כל סינון הופך ל-`NULL`
   ומחזיר רשימה ריקה בלי שגיאה. **פרמטרים מיקומיים `?` בלבד.**
3. **`LIMIT`/`OFFSET` חייבים להיות משובצים בטקסט** כמספרים שלמים מאומתים —
   מספר JS מגיע כ-float ו-SQLite זורקת "datatype mismatch".

אם אתה כותב קוד שנוגע במסד, בדוק אותו מול השלושה האלה.

## החלטות שלא לפתוח מחדש

- **לא עוקפים הגנת בוטים.** רפאל (Reblaze), התעשייה האווירית ו-AllJobs
  (hCaptcha) חסומים בכוונה על ידי בעליהם. רפאל מנוטרת דרך אדפטר `manual` —
  משרות מוזנות ידנית. זו החלטה, לא מגבלה טכנית שצריך לפתור.
- **אין שליחת מיילים.** כל ספק דיוור דורש כתובת מגורים בהרשמה, ואני לא
  מוכנה למסור אותה. שחזור סיסמה בנוי במלואו אבל כבוי; הקישור מודפס ללוג.
- **מכפיל אבטחה:** כל שאילתה שנוגעת בנתונים אישיים מקבלת `userId` ראשון
  וקורסת בלעדיו (`requireUser`). מבקר לא מחובר מקבל סנטינל `GUEST` מפורש.
  חיפוש המשרות פומבי; מעקב ההגשות דורש חשבון.

## המצב כרגע

עובד: חיפוש, סינון (חברה/ניסיון/היקף/מיקום), עימוד, חשבונות, מעקב הגשות,
נגישות (WCAG AA, מצב לילה, ניגודיות גבוהה, RTL), חסם קצב להתחברות, זיהוי
משרות שנסגרו עם שער שפיות שמונע מסריקה שבורה למחוק חברה שלמה.

~2,500 משרות מ-37 חברות. 280 טסטים (`node --test`), 0 נכשלים.

**פערים ידועים:** אין מדיניות פרטיות (חסם משפטי לפני פרסום), אין אימות
מייל, אין שחזור סיסמה למשתמשים, האדפטר של IBM מחזיר 0 משרות, ואמדוקס,
SanDisk ו-KLA עדיין לא נוספו.

## איך אני עובדת

פיתוח על **Windows/PowerShell**. תסביר לי דברים עם אנלוגיות — ככה אני
מבינה הכי טוב. אני מעדיפה תשובות קצרות וישירות, ושתגיד לי כשאני טועה
במקום להסכים איתי.
